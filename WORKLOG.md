# 作業ログ

書式：`- YYYY-MM-DD 何を / 相手・宛先 / 結果 → 次のアクション`

## 決めたこと

- 2026-09-11 **月例WSの受講者用資料は、今後毎回ここ（GitHub Pages）で配る**（宮崎さん指示「今後は毎回そうして」）。
  アーティファクトは使わない ── ①claude.aiのアカウントが必要 ②会員限定にできない
  ③16MB上限のため画質を落とさないと入らない、の3つで配布に向かない
- 2026-09-11 public にする（宮崎さん判断「問題ない」）。ただし `robots.txt` と `noindex` で検索には出さない

## やったこと

- 2026-09-11 リポジトリ作成、2026年9月「睡眠の応用神経学」122枚を公開 / — / 画像は原寸1280×1080・外部ファイル。index.html は113KB
- 2026-09-11 過去回6本を追加（時系列）/ 宮崎さん / Driveの同じフォルダ（`1qLM-DM8nMEef7M9h2NWDYTAXmVHkeNjT`）にPDF7本。うち `2026-07`（靱帯22頁）は `2026-08`（59頁）の前半22頁と**完全一致**だったので載せず6本にした。全7回・440枚・画像36.5MB
  - 🔴 **開催月は推測。** カレンダーにテーマが無く、Driveのアップロード日も同日に2本ある回があるので1対1に決まらない。時期はアップロード日から置いた。要訂正
  - 過去回は **PDFが正本なので用語集と章が作れない**（用語集は `source.md` から自動生成しているため）。目次・送り・スワイプまで
  - 各ページの見出しはPDFのテキスト1行目から取った（推測しない）。BFRだけ画像書き出しで文字が無く 3/67
- 2026-09-11 全8ページにパスコード（fnt0915）を設定 / 宮崎さん指示 / 仕込みは `_gate.html` の1箇所・`apply_gate.py` で全ページへ。パスコードは平文で置かずSHA-256のみ。4方向で実測（ロックされる／間違いを弾く／正しいと開く／覚えている）。🔴 **鍵ではない** ── 静的ホストなので画像URLを直接叩けば通らずに見られる。本当に鍵をかけるならVercel等でサーバー側認証
- 2026-09-11 🔴 Pagesのビルド待ちを `status == built` だけで見ていたので、**前回のビルドを見て即座に抜けた** / — / 結果、まだ配信されていないページを検算して「札が入っていない」と誤報した。`scripts/wait_pages.sh` を作り、**いまのHEADのSHAと一致した上でbuilt** になるまで待つようにした
- 2026-09-11 パスコードを外した / 宮崎さん判断「サーバー認証だと開けない人が出てこない？パスコードが必要ないなら其のほうが楽」 / **そのとおり。** ①静的ホストなので画像URL直叩きで通り、何も守っていない ②リンクを踏むのは大半がアプリ内ブラウザで、記憶が消えて毎回聞かれる → 守らないものに、開けない人と問い合わせを払うのは損。**本当の鍵は会員サイト（SWPM）側に既にある**ので、会員限定ページからリンクすれば実質的に会員しか辿れない。仕込みは `_gate.html` に残し、`apply_gate.py [--remove]` で出し入れできる（両方向を実測）
